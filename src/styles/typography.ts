import styled  from "styled-components";

export const TitleLogo = styled.h1`
    color: var(--white);
    font-weight: 700;
    font-size: var(--title1);
    
    & > span {
        color: var(--colorA);
    }
`